import UIKit

final class PayBlockView: UIView {
    private let agreementTextLabel: UILabel = {
        let label = UILabel()
        return label
    }()

    private let agreementLinkLabel: UILabel = {
        let label = UILabel()
        return label
    }()

    private let payButton: UIButton = {
        let button = UIButton()
        return button
    }()
}
